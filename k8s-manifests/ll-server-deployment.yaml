apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yaml -o ./k8s-manifests
    kompose.image-pull-policy: Never
    kompose.version: 1.38.0 (a8f5d1cbd)
  labels:
    io.kompose.service: ll-server
  name: ll-server
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: ll-server
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yaml -o ./k8s-manifests
        kompose.image-pull-policy: Never
        kompose.version: 1.38.0 (a8f5d1cbd)
      labels:
        io.kompose.service: ll-server
    spec:
      containers:
        - args:
            - uv
            - run
            - load_listener
          env:
            - name: PYTHONUNBUFFERED
              value: "1"
          envFrom:
            - configMapRef:
                name: env-client-init-env
          image: docker.io/library/compal_intern_assignment3-ll-server:latest
          imagePullPolicy: Never
          name: load-listener
          ports:
            - containerPort: 8000
              protocol: TCP
      restartPolicy: Always
